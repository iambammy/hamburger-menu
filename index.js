function printName(age){
    // define a name
    const name = "Trey C."
    // try showing the name and age in the sentence
    console.log(`Hello y'all my name is ${name} and i'm ${age} years old....`);

}

printName(17);